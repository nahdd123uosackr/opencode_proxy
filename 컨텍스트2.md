# opencode-proxy 컨텍스트 2 (2026-09-11, 오후 세션)

이 문서는 `컨텍스트.md`(오전 — res/chat/mes 프로토콜 분리 passthrough 설계 전환)에 이어지는 **같은 날
오후 세션**의 작업 기록이다. `컨텍스트.md`가 끝난 지점(커밋 `87494ec`)부터 시작해서 muse-spark 관련
버그 3건(P24/P27/P28) 수정, Kilo 전용 네이티브 라우트 신설(P26), GET /models 무료 필터 누락 수정(P25),
Orkestr.eu 배포 시도(실패, 플랫폼 장애)까지 다룬다.

## 0. 세션 커밋 목록 (`87494ec` → `HEAD`)

```
5712b94 fix: strip auto-injected image_generation tool on /res/v1/responses (P24)
165197f feat: add /kilo/v1/models and /kilo/v1/chat/completions native routes
3f34892 docs: record P25 (models free-filter gap) and P26 (Kilo native routes)
526d3cf docs: record P27 (muse-spark duplicate registration causing 504s)
4bfc1f5 fix: enforce 131072 max_output_tokens floor for muse on /res/v1/responses (P28)
```

모두 `origin/main`에 push 완료. 상세 서술은 `docs/문제_해결.md`의 P24~P28, 요약 표는 `docs/skill.md`
§6, res/chat/mes 설계 자체와의 연결은 `docs/11_res_chat_mes_passthrough_및_cliproxyapi_연동.md`에
"후속 확장"으로 정리돼 있다. 이 문서는 그것들을 한 흐름으로 다시 읽을 수 있게 모은 것.

## 1. P24 — CLIProxyAPI codex 실행기의 `image_generation` tool 자동 주입 (muse-spark `/res/v1/responses` 400)

**증상**: `opencode-res`/`opencode-zen1-res`(CLIProxyAPI `codex-api-key` → `/res/v1/responses`) 경유
muse-spark 호출이 항상 `[400] Unsupported tool type: 'image_generation'`로 실패. `컨텍스트.md` §9
"남은 이슈 2"에 이미 발견은 돼 있었지만 미해결 상태였음.

**원인**: CLIProxyAPI의 codex 실행기가 `/responses` 요청에 `tools:[{type:"image_generation"}]`을
기본으로 자동 주입(클라이언트가 보낸 tools엔 없음). zen은 이 tool 타입 자체를 지원하지 않아 무조건 400.
CLIProxyAPI 쪽 `payload.filter`(gjson/sjson path 제거) 옵션으로 막을 수 있는지 조사했으나 배열 요소
조건부 제거 문법이 불확실해 opencode-proxy 쪽에서 직접 필터링하기로 결정.

**수정**: `/res/v1/responses` passthrough 라우트에서 `tools` 배열에 `image_generation` 타입이 섞여
있을 때만 그것만 제거(없으면 원본 그대로 바이트 단위 passthrough 유지). 3개 사본(`api/index.js`,
`vercel/api/index.js`, `deno/main.ts`) 동일 적용.

**검증**: pool 경유 직접 호출 200 성공(응답 `tools:[]`) + CLIProxyAPI 경유 실제 재현 경로
(`zen/muse-spark-1.3-contributor-free`) 재호출 → `status:"completed"` 200.

## 2. P25 — `/res|chat|mes/v1/models`가 유료 모델까지 노출 (무료 필터 누락)

`GET /res|chat|mes/v1/models`는 POST 쪽(바디 무변형 passthrough) 원칙을 그대로 적용해서 zen
`/models` 전체(70개, 유료 포함)를 필터 없이 반환하고 있었다 — 레거시 `GET /v1/models`는 키 종류와
무관하게 항상 무료만(`getFreeModelsExpanded()`) 반환하는 것과 불일치.

**수정**: 응답의 `data` 배열을 `id.endsWith('-free') || KNOWN_FREE_EXTRA.has(id)`(레거시와 동일 기준)로
필터링. `id`는 `opencode/` 접두사 없이 zen 네이티브 형식 유지. 결과: 세 라우트 전부 8개(무료 `-free` 7개 +
`big-pickle`)만 반환.

## 3. P26 — Kilo 키가 `/res|chat|mes/*`에서 무시됨 (버그 아님) → `/kilo/v1/*` 전용 라우트 신설

**시작된 질문**: "Kilo 키 넣으면 Kilo 모델만, Zen 키 넣으면 Zen 모델만 반환되나?" 실측 결과 레거시
`/v1/models`(+`/v1/chat/completions`)는 정확히 그렇게 분기(Kilo 키→Kilo 22개, Zen 키→Zen 8개, 키
없음→결합)하지만, 신규 `/res|chat|mes/*`는 Kilo 키를 넣어도 항상 zen만 반환.

사용자가 "예전엔 Kilo도 됐었어"라고 반박 → `git log -S"KILO_BASE"`로 전체 히스토리 대조한 결과 Kilo
라우팅 코드는 한 번도 수정된 적 없음을 확인, 즉 회귀가 아니라 **애초에 `/res|chat|mes/*`가 zen 전용으로
설계**됐던 것(POST 쪽도 Kilo 분기 자체가 없음 — Kilo는 zen과 무관한 별도 서비스라 "zen 모델별 엔드포인트
불일치"라는 이 라우트군의 존재 이유 자체가 적용 안 됨).

**신설**: 사용자 요청으로 Kilo 전용 네이티브 라우트 2개 추가.

| 경로 | 동작 |
|---|---|
| `GET /kilo/v1/models` | `getKiloFreeModels()` 재사용, 무료만, `kilo/` 접두사 벗기고 반환 |
| `POST /kilo/v1/chat/completions` | `KILO_BASE + '/chat/completions'`로 직접 forward. `kilo/` 접두사 방어적 스트립(레거시 호환), 클라이언트 `Authorization` 있으면 전달·없으면 키 없이 무료 모델 허용, `reasoning_effort`/`reasoningEffort` 무조건 제거(P17 재발 방지) |

`isNativeProtocolPrefix`에 `/kilo/` 추가. 검증: `/kilo/v1/models` 22개(무료만) · `/kilo/v1/chat/completions`
키 없이 `kilo-auto/free`·`dots-studio/dots-3-note-preview:free` 200 성공 · `kilo/` 접두사 입력도 정상
스트립.

**흥미로운 발견**: CLIProxyAPI config.yaml을 확인하던 중 `kilo1` provider의 `base-url`이 이미
`http://192.168.200.2:8770/kilo/v1`(우리 신규 라우트)로 바뀌어 있고, `openkilo`라는 이름의 두 번째
provider도 동일 경로로 등록돼 있는 게 발견됨(둘 다 22개 모델, `openkilo`는 api-key-entries 없이 키리스
모드로 보임) — **누가·언제 등록했는지 이 세션에서 추적하지 않았음, 미확인 상태로 남음.** 다음 세션에서
확인 필요(§8 참고).

## 4. Orkestr.eu 배포 시도 — 실패 (플랫폼 장애, 우리 쪽 문제 아님)

사용자가 API 토큰(`ork_...`)을 주고 orkestr.eu 배포를 요청. 조사 결과:

- Orkestr는 EU 호스팅 컨테이너 배포 플랫폼(`orkestr-cli`, PyPI 패키지, Python 3.10+). 오라클2에
  `/root/.orkestr-venv`에 격리 설치.
- 이미 `opencode-proxy-2`라는 프로젝트가 이 계정에 존재(우리 GitHub repo `main` 브랜치 연결,
  Dockerfile 자동 감지, `opencode-proxy-2.orkestr.run`) — 사용자가 세션 전에 콘솔에서 이미 한 번
  시도했던 흔적.
- `orkestr deploy opencode-proxy-2`로 2회 재시도, **매번 정확히 같은 에러로 빌드 단계 실패**:
  ```
  host daemon returned 502: sandbox boot failed: [Errno 28] No space left on device:
  '/var/lib/orkestr-sandbox/rootfs/builder.ext4' -> '/srv/jailer/firecracker/sbx-.../root/rootfs.ext4'
  ```
  `clone`·`analyze` 단계는 매번 정상(Dockerfile·포트 8080 정상 감지) — **Orkestr의 빌드 노드
  (`sbg-node-01`) 디스크가 꽉 찬 것으로, 우리 쪽 코드/설정 문제가 전혀 아님.** CLI에는 로컬 빌드 이미지를
  직접 push하는 우회 경로도 없음(레지스트리 명령은 조회/삭제만 지원).
- **미해결 상태로 보류.** 나중에 재시도하거나 Orkestr 측에 문의 필요. `redeploy.sh`의 44개 pool
  fleet와는 완전히 별개(Orkestr는 pool에 아직 추가 안 됨 — 배포 자체가 안 됐으므로).
- 참고로 pool엔 기존부터 `opencode-proxy.orkestr.run`(다른 프로젝트, git 자동배포 방식)이 이미 1개
  들어있음 — `opencode-proxy-2`와는 별개.

사용자가 이어서 "suga 전체와 orkestr opencode-proxy-2를 pool에서 삭제해"라고 요청했으나, pool
upstream 47개 전체와 문서를 뒤져도 "suga"라는 이름은 어디에도 없었음(오탈자/다른 의미로 추정) → 확인
질문 후 사용자가 "아니 됐다"로 철회. **pool은 이 요청으로 인해 변경되지 않았음.**

## 5. P27 — CLIProxyAPI `openai-compatibility`에 muse-spark 중복 등록 (`/chat/completions` 경유 504)

**증상**: OmniRoute `mycli`(provider, `base_url: http://omniroute-scsi:8317/v1` = CLIProxyAPI 자신)
경유 `mycli/muse-spark-1.3-contributor-free` 호출이 `[504]: Direct response did not start within
30000ms`로 실패.

**추적 경로**: `/opt/opencode-pool/pool.log`에서 같은 시각 `/chat/v1/chat/completions`로 muse-spark
요청이 실제로 들어온 걸 확인 → CLIProxyAPI `config.yaml`을 열어보니 `openai-compatibility`(chat) 섹션의
`opencode-chat`·`opencode-zen1-chat` **두 provider 모두 `models:` 목록에 muse-spark 2개가 잘못
등록**돼 있었음(`codex-api-key`, 즉 `/responses` 전용 섹션에는 이미 올바르게 등록돼 있어 중복이 문제).
muse-spark는 `/chat/completions`에서 항상 hang하는 게 이미 알려진 문제(P9/P13 계열, res/chat/mes
분리를 하게 된 근본 이유 그 자체).

**수정**: `openai-compatibility` 쪽 두 provider의 `models:`에서만 muse-spark 2개 항목 제거(`codex-api-key`
쪽은 그대로 유지). `docs/11_...md`의 기존 안전 패치 절차(로컬로 가져오기 → 파이썬 텍스트 치환만, PyYAML
dump 금지 → 파싱 검증 → oracle1 경유 scp → kubectl cp → 사전 pod 내 백업) 그대로 재사용, 정확히 2곳
발견·2곳 제거를 스크립트 assert로 확인.

**배포 사고 — 자동 재기동 없음**: `sh -c 'kill <pid>'`로 CLIProxyAPI를 종료시켰는데 **부모 프로세스
(omniroute 메인, PID 20)가 자동으로 재기동해주지 않았다** — `/proc` 재확인 결과 완전히 죽은 채로 남아
`:8317` 전체가 잠깐(~10초) 다운. `sh -c 'nohup /app/data/bin/cliproxyapi --config
/app/data/services/cliproxy/config.yaml > /tmp/....log 2>&1 &'`로 직접 백그라운드 기동해서 복구.
`kubectl exec pod -- kill <pid>`처럼 `sh -c`로 안 감싸면 `kill` 바이너리 자체가 컨테이너에 없어 즉시
실패(`exec: "kill": executable file not found`) — 반드시 `sh -c 'kill <pid>'`로 감쌀 것.

**검증**: `POST /v1/chat/completions` `model:"opencode-zen/muse-spark-*"` → 수정 후 즉시 `400
model_not_found`(무한 대기 사라짐). `POST /v1/responses` `model:"zen/muse-spark-*"` → 여전히 `200
completed` 정상.

## 6. "openai-chat" 프로토콜 표시 관련 오해 해소

사용자가 OmniRoute에 새 provider `openprox-res`를 `api_type: responses`로 등록했는데 로그에 "요청
프로토콜: OpenAI-Chat"이 떠서 잘못된 게 아닌지 질문. `call_logs` 테이블(`storage.sqlite`)을 직접 조회해
`source_format`(클라이언트→OmniRoute)와 `target_format`(OmniRoute→provider) 필드가 별도로 존재함을
확인 — 로그의 "프로토콜"은 `source_format`(클라이언트가 OmniRoute를 호출한 방식)이고, `target_format:
"openai-responses"`가 실제 provider로 전달되는 방식이라 `api_type: responses` 설정과 정확히 일치.
**등록은 정상, 혼동일 뿐이었음.**

## 7. P28 — `/res/v1/responses`에 작은 `max_output_tokens`로 muse 호출 시 502 empty stream

**증상**: 위 `openprox-res`로 `max_output_tokens:64`짜리 muse-spark 요청을 보내면 **pool의 47개
업스트림 전부**(오라클1/오라클2 로컬 포함)에서 동일하게 `bytes=1961` "empty stream"으로 판정돼 failover
→ 최종 502. 노드 장애가 아니라 요청 자체가 구조적으로 실패하는 패턴.

**원인**: muse-spark는 실제 텍스트 출력 전에 내부 reasoning으로 먼저 토큰을 소모. `max_output_tokens:64`
같은 작은 예산은 reasoning만으로 다 소진되고 `output:[]`(빈 출력)인 채로 HTTP 200 "성공" 종료
(`incomplete_details.reason:"max_output_tokens"`). 레거시 변환형 `/v1/responses`(`applyMuseDefaults()`)는
muse에 `max_output_tokens ≥ 131072`를 강제해서 이 문제를 막지만, `/res/v1/responses`는 "바디 무변형"
원칙이라 이 안전장치가 없었음.

**수정**: `/res/v1/responses`에서 model이 muse 계열일 때만 `max_output_tokens`가 131072 미만이면
(기존값과 131072 중 큰 값으로) 상향. P24의 image_generation 필터와 같은 성격의 최소 예외. 두 패치
(tool 제거 + 토큰 하한)가 같은 요청에 동시 필요할 수 있어 `patched` 객체에 누적 적용 후 한 번만
재직렬화하도록 리팩터링.

**검증**: 유닛 테스트 4케이스(작은 예산→상향/큰 예산→보존/non-muse→무변형/두 패치 동시 적용) 전부 통과.
원본 실패 요청("Calculate 34717+50086") 재현 → 수정 전 `output:[]`, 수정 후 `status:"completed"` +
`max_output_tokens:131072`(64에서 상향 확인) + 정답 `"84803"` 정상 출력.

## 8. 배포 인프라 관련 실전 메모 (다음 세션에서 바로 쓸 것)

- **`redeploy.sh`는 `bash redeploy.sh`로 실행할 것** — `/root/opencode_proxy`가 S3 마운트라 `chmod +x`가
  안 먹어서 `./redeploy.sh`는 `permission denied`(exit 126)로 실패한다.
- 이번 세션에서 P24/P25+P26/P27(config.yaml만, 코드 배포 없음)/P28, 총 4번의 `redeploy.sh all`
  풀배포(각 44/44 성공) + v3/v4 URL 재교체 + `opencode-pool` 재시작을 반복. v3/v4는 배포마다 URL이
  바뀌므로 **매번 잊지 말고 pool upstream 갱신할 것**(안 하면 pool이 죽은 URL로 계속 시도하다 다른 노드로
  failover만 하느라 낭비됨 — 치명적이진 않지만 비효율).
- **CLIProxyAPI(`/app/data/bin/cliproxyapi`, 포트 8317)는 kill해도 자동 재기동 안 됨** — 재시작 시
  반드시 `sh -c 'nohup ... &'`로 직접 재기동하고, 재기동 후 `/proc` 스캔으로 살아있는지 재확인할 것
  (§5 P27 참고).
- `kubectl exec pod -c omniroute -- <bare-binary>` 형태는 `sh -c`로 감싸지 않으면 셸 내장 명령
  (`kill` 등)이 실패한다. 이 컨테이너엔 `ps`/`curl`/`kill` 바이너리가 없다 — 전부 `node -e "fetch(...)"`
  또는 `sh -c` 내장 명령으로 우회.
- **미확인 상태로 남은 것**: CLIProxyAPI config.yaml의 `kilo1` base-url이 이미 우리 신규 `/kilo/v1`을
  가리키도록 바뀌어 있고 `openkilo`라는 정체불명의 두 번째 provider가 추가돼 있음 — 이 세션에서 누가
  언제 왜 등록했는지 확인 안 함. 다음에 만지기 전에 먼저 이력 확인할 것.
- Orkestr.eu `opencode-proxy-2` 배포는 여전히 실패 상태(플랫폼 디스크 부족) — 재시도하려면
  `/root/.orkestr-venv/bin/orkestr --token <ork_...> deploy opencode-proxy-2 --timeout 300`.

## 9. P29 — CLIProxyAPI config.yaml 전체 초기화 (openproxy 관련 provider 전부 소실) → pod 내 백업 파일로 복구

**증상**: 다음날(2026-09-12) 세션 시작 시 사용자가 "어제 등록한 openproxy 관련 프로바이더가 cliproxyapi에서
사라졌다"고 보고. `cliproxy-master-key`(일반 API 키, `remote-management.secret-key`와는 별개 — 관리 API
평문 비밀번호 문제는 여전히 미해결)로 확인.

**조사 결과 — 예상보다 훨씬 큰 범위의 초기화**: 단순히 "openproxy 관련 provider"만 사라진 게 아니라
`config.yaml`에서 `codex-api-key`/`claude-api-key` 최상위 섹션 자체가 통째로 없어졌고(`openai-compatibility`엔
기존부터 있던 `cline1`/`nvidia`/`openrouter`/`bazaarlink`/`ollama`/`gemini` 6개만 남고 `opencode-chat`/
`opencode-zen1-chat`/`kilo1`/`openkilo`가 전부 사라짐), `credential-concurrency`/`credential-in-flight`/
`plugins.model-router`/`ws-auth` 등 다른 최상위 섹션도 함께 사라져 있었다. 더 나아가 bifrost 자체의
`config_providers`/`config_keys` 테이블(`config.db`)에서도 `opencode-zen1`, `kilo-gateway` 커스텀
프로바이더가 사라져 있어(남은 건 2026-09-04/05에 만들어진 오래된 6개뿐) **CLIProxyAPI 쪽뿐 아니라
bifrost/OmniRoute 백엔드 데이터 자체가 훨씬 이전 시점(2026-09-04~05 무렵으로 추정) 상태로 되돌아간 것**으로
보인다. **누가/무엇이 이 초기화를 일으켰는지는 이번에도 추적하지 못했다** — pod 자체는 재시작(`RESTARTS: 0`)
안 됐고 `startTime: 2026-09-11T14:07:04Z`인데, config.yaml의 마지막 수정 시각(pod 안 파일 mtime)도 같은 날
14:31 UTC(=23:31 KST)라 pod 교체가 아니라 **살아있는 pod 안에서 파일/DB가 직접 되돌려진 것**으로 보인다 —
원인 미상, 다음 세션에서도 계속 주시 필요.

**복구 경로 — 다행히 pod 안에 지난 세션(§5 P27, §7 P28 작업 도중)의 안전 패치 절차 백업 파일이 그대로 남아
있었다**:

```
config.yaml.bak-20260911-183836-pre-cleanup-old-providers   (39056B, claude-api-key에 실제 mes 데이터 있음)
config.yaml.bak-20260911-225208-pre-muse-chat-removal        (10776B, P27 직전 — opencode/kilo 전부 있음)
```

`pre-muse-chat-removal`에서 `openai-compatibility`(`opencode-chat`/`opencode-zen1-chat`/`kilo1`/`openkilo`,
실제 zen sk- 키·kilo JWT 키 그대로 포함)와 `codex-api-key`(zen/opencode-zen1 prefix, muse-spark 4종,
키리스) 전체를, `pre-cleanup-old-providers`에서 `claude-api-key`(zen/opencode-zen1 prefix, claude-* 12종
+ 빈 목록)를 가져와 재조합했다. 키 값을 손으로 옮겨 적지 않고 **Python으로 두 백업을 `yaml.safe_load`한 뒤
필요한 항목만 그대로 추출·재조합**해서 오타/누락 위험을 없앴다(라이브 config는 여전히 전체 재직렬화하지 않고
새 항목만 텍스트로 append하는 기존 안전 패치 원칙 유지). `opencode-chat`/`opencode-zen1-chat`의 `models`
목록에서는 P27이 제거했던 muse-spark 2종을 다시 빼서 재등록(재발 방지).

**검증**: 재기동(`kill` → `sh -c 'nohup ... &'`, §8 절차 그대로) 후 `/v1/models` 응답에서
`opencode-zen/*` 6개, `kilo/*` 22개, `zen/*`(muse 4 + claude 12) 확인 + `opencode-zen/big-pickle`로 실제
`/v1/chat/completions` 호출 200 성공까지 확인. 배포 로그에도 `2 Claude API keys + 2 Codex keys + 198
OpenAI-compat` 클라이언트 로드로 정확히 반영됨.

**흥미로운 부수 발견**: `claude-api-key`에서 `models: []`로 등록해 둔 `opencode-zen1` 항목이 실제로는
CLIProxyAPI가 업스트림에서 모델 카탈로그를 자동 탐지해서 16개(claude-sonnet-4-5-20250929 등 실제 zen 카탈로그
id)를 채워 넣고 있었다 — `모델을 지어내지 않고 빈 목록으로 등록`(§docs/11) 당시 예상과 달리 `models: []`가
"모델 없음"이 아니라 "자동탐지에 맡김"으로 동작하는 것으로 보인다. `opencode-zen1-mes에 무료 모델 없음` 이슈
재검토 시 참고할 것(단, 이건 우리 프록시의 무료 필터를 거치지 않은 CLIProxyAPI 레벨 카탈로그라 유료 모델도
섞여 있을 수 있음 — 실사용 전 확인 필요).

**미확인으로 남는 것**: 이번 초기화의 원인. 다음 세션 시작 시 `config.yaml` 최상위 섹션 구성과 bifrost
`config_providers` 개수를 먼저 확인해서 또 초기화됐는지부터 체크할 것.

## 10. P30 — `/res|chat|mes/v1/models` 프로토콜 무관 동일 목록 반환 → zen 공식 문서 기반 동적 분류로 수정

**시작된 질문**: "res 엔드포인트엔 responses를 쓰는 모델만, chat 엔드포인트엔 chat/completions를 쓰는
모델만 나오게 할 수 있나? 어떤 모델이 어떤 포맷을 쓰는지 확인하는 방법이 있나?" — 확인해보니 P25 이후에도
세 라우트가 여전히 프로토콜 구분 없이 동일한 무료 목록을 반환하고 있었다(설계 의도는 `docs/11`에 이미
있었지만 구현에서 빠짐).

**1차 시도 — 이름 패턴**: zen의 `/models` 응답엔 엔드포인트 메타데이터가 없어서 `claude-*`→messages,
`muse-spark*`→responses, 나머지→chat으로 분류. 실제 zen 모델 70개를 나열해 겹침 없이 깔끔히 나뉘는 걸
확인하고 이 방식으로 1차 구현.

**공식 문서 대조 — 오분류 발견**: 사용자가 opencode 공식 문서
(`opencode.ai/docs/ko/zen/`, 원본은 GitHub `anomalyco/opencode`의
`packages/web/src/content/docs/zen.mdx`)를 근거로 제시. WebFetch 요약 대신 raw HTML/raw.githubusercontent
원문을 직접 파싱(정적 마크다운 표라 정규식만으로 완전 파싱 가능, 69행)한 결과, **qwen***도 messages
전용이고 **gemini-***는 셋 중 어디에도 안 속하는 별도 포맷(`/v1/models/{id}`, `@ai-sdk/google`)이었다 —
이름 패턴 가정이 틀렸음을 확인. 다만 qwen/gemini 둘 다 유료 전용(무료 버전 없음)이라 이 오분류가
무료-필터 이전 단계라 실사용엔 영향 없었음(프로젝트가 가진 키가 전부 무결제라 실측 검증 자체도 불가 —
`CreditsError`로 라우팅 전에 막힘).

**한/영 문서 비교**: 사용자 제안으로 영문판(`packages/web/src/content/docs/zen.mdx`, ko 없이)과 비교 —
모델ID/엔드포인트 컬럼은 byte-identical(69행)하지만 영문판 섹션 헤더(`## Endpoints`/`### Models`/
`## Pricing`)가 ASCII라 정규식 앵커링이 한글판보다 안전해서 영문판을 파싱 소스로 채택.

**서버 소스까지 확인**: 사용자가 `anomalyco/opencode`의 `packages/llm`(클라이언트 라이브러리)을 가리켰지만
실제로 필요한 건 `packages/console/app/src/routes/zen/`(zen 게이트웨이 **서버** 소스)이었다. 확인 결과:
- 각 엔드포인트가 고정 `format`을 선언(`responses.ts`→`"openai"`, `messages.ts`→`"anthropic"`,
  `chat/completions.ts`→`"oa-compat"`, `google.ts` 헬퍼로 `"google"`도 존재)
- `handler.ts`의 `validateModel()`이 모델의 `formatFilter`와 요청 포맷이 다르면 변환 없이 즉시
  `ModelError: modelFormatNotSupported`로 거부
- 즉 우리가 겪은 "muse-spark를 chat/completions로 부르면 실패"는 zen 서버가 강제하는 하드 게이트를
  실측한 것이었고, 우리 res/chat/mes 3분리 설계와 정확히 같은 메커니즘임이 코드로 확인됨
- 이 `formatFilter`가 담긴 실제 레지스트리(`zenData.models`)는 `@opencode-ai/console-core`라는 비공개
  내부 패키지에 있어 이 공개 저장소엔 없음 — `zen.mdx` 표가 가장 가까운 공개 대리 데이터

**최종 수정**: `getZenEndpointMap()`이 영문 zen.mdx를 fetch해서 "Endpoints" 표를 정규식 파싱, 모델→포맷
맵을 6시간 캐시. `classifyZenModelProtocol()`이 이 맵을 우선 참조하고, 맵에 없는 모델(문서가 API보다
며칠 뒤처질 수 있음 — 실제로 `muse-spark-1.2-contributor-free`/`deepseek-v4-flash-free`가 이 표에
빠져 있었다)이나 fetch/파싱 실패 시엔 기존 이름 패턴으로 폴백. `/res|chat|mes/v1/models` 세 핸들러가 이
분류기를 쓰도록 교체, `gemini-native`/미분류는 세 라우트 어디에도 노출 안 함. `api/index.js`/
`vercel/api/index.js`/`deno/main.ts` 3곳 동일 적용, `node --check` 통과.

**검증**: 실제 zen `/models` 70개에 최종 필터 시뮬레이션 → res 2개(muse-spark 무료 2종)/mes 0개/chat
6개(big-pickle 등) — **수정 전(하드코딩 패턴)과 완전히 동일한 출력**, 회귀 없음.

상세: `문제_해결.md` P30, 설계 문서 갱신은 `docs/11_...md` "후속 확장" 참고.

## 11. 남은 이슈 (컨텍스트.md §9에서 이어지는 것 + 이번 세션 신규)

1. `opencode-zen1-mes`에 무료 모델 없음 — 여전히 미해결(컨텍스트.md §9-1과 동일, 결제된 zen 키 필요).단,
   §9 P29 "흥미로운 부수 발견" 참고 — `models: []`가 자동탐지로 동작해 유료 모델이 섞여 노출될 수 있음.
2. CLIProxyAPI 관리 API 접근 불가(평문 비밀번호 없음) — 여전히 미해결.
3. **Orkestr.eu `opencode-proxy-2` 배포 실패** — 플랫폼 자체 디스크 문제, 우리 쪽에서 당장 해결 불가.
4. **CLIProxyAPI `kilo1`/`openkilo` provider 이력 미확인** — 위 §3, §8 참고. P29 복구로 두 provider가
   같은 배치로(같은 백업 시점에 함께) 등록됐던 것은 확인됐지만 "누가/왜 openkilo를 추가했는지"는 여전히
   미상.
5. pool.js 변경사항 git 미커밋 이슈는 컨텍스트.md §9-4에서 이미 언급 — 이번 세션에서는 pool.js 자체를
   건드리지 않음(upstream URL만 drop-in `.conf` 파일로 갱신, `pool.js` 소스는 무변경).
6. **신규(P29)**: CLIProxyAPI config.yaml/bifrost config.db가 세션 사이에 원인불명으로 예전 상태로
   되돌아가는 현상 — 재발 감시 필요, 근본 원인 미상.
